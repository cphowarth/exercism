package hamming

import (
	"fmt"
	"errors"
)

// Distance - work out the hamming distance between 2 DNS strands
func Distance(a, b string) (int, error) {
	fmt.Println(a, b)
	var distance int = 5
	return distance
	// , error = errors.New("testing an error")
}
